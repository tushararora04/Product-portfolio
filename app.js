import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.161.0/build/three.module.js';const h=document.querySelector('#scene');if(h){const s=new THREE.Scene(),c=new THREE.PerspectiveCamera(50,1,.1,100),r=new THREE.WebGLRenderer({alpha:true,antialias:true});r.setPixelRatio(Math.min(devicePixelRatio,2));h.appendChild(r.domElement);c.position.z=5;const g=new THREE.Group();s.add(g);g.add(new THREE.Mesh(new THREE.IcosahedronGeometry(1.28,4),new THREE.MeshBasicMaterial({color:0xd6fa00,wireframe:true})),new THREE.Points(new THREE.IcosahedronGeometry(1.35,4),new THREE.PointsMaterial({color:0xff5b2e,size:.045})));const ring=new THREE.Mesh(new THREE.TorusGeometry(1.75,.013,8,100),new THREE.MeshBasicMaterial({color:0x101112}));ring.rotation.x=1.12;g.add(ring);let x=0,y=0;addEventListener('pointermove',e=>{x=e.clientX/innerWidth-.5;y=e.clientY/innerHeight-.5});function z(){const b=h.getBoundingClientRect();r.setSize(b.width,b.height);c.aspect=b.width/b.height;c.updateProjectionMatrix()}addEventListener('resize',z);z();(function l(){g.rotation.y+=.004+(x*.13-g.rotation.y)*.01;g.rotation.x+=(y*.15-g.rotation.x)*.015;ring.rotation.z+=.002;r.render(s,c);requestAnimationFrame(l)})()}
